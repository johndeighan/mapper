// Generated by CoffeeScript 2.7.0
  // RunTimeStack.coffee
import {
  undef,
  assert,
  croak
} from '@jdeighan/coffee-utils';

// ---------------------------------------------------------------------------
export var RunTimeStack = class RunTimeStack {
  constructor() {
    this.lStack = [];
  }

  // ..........................................................
  TOS() {
    if (this.lStack.length === 0) {
      return undef;
    } else {
      return this.lStack[this.lStack.length - 1];
    }
  }

  // ..........................................................
  push(uobj, level, lineNum, hUser) {
    return this.lStack.push({
      uobj,
      level,
      lineNum,
      hUser,
      parent: this.lStack.TOS()
    });
  }

  // ..........................................................
  pop() {
    var h;
    h = this.lStack.pop();
    return [h.uobj, h.level, h.lineNum, h.hUser, h.parent];
  }

};
