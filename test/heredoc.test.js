// Generated by CoffeeScript 2.7.0
// heredoc.test.coffee
var HereDocReplacer, HereDocTester, MatrixHereDoc, UCHereDoc, UCHereDoc2, docTester, replacer;

import {
  LOG,
  assert,
  croak
} from '@jdeighan/base-utils';

import {
  dbg,
  dbgEnter,
  dbgReturn,
  setDebugging
} from '@jdeighan/base-utils/debug';

import {
  UnitTester,
  utest
} from '@jdeighan/unit-tester';

import {
  undef,
  isString,
  extractMatches,
  CWS,
  OL,
  defined,
  notdefined
} from '@jdeighan/coffee-utils';

import {
  blockToArray
} from '@jdeighan/coffee-utils/block';

import {
  undented
} from '@jdeighan/coffee-utils/indent';

import {
  firstLine,
  remainingLines
} from '@jdeighan/coffee-utils/block';

import {
  lineToParts,
  mapHereDoc,
  addHereDocType,
  isHereDocType,
  BaseHereDoc
} from '@jdeighan/mapper/heredoc';

// ---------------------------------------------------------------------------
utest.truthy(20, isHereDocType('one line'));

utest.truthy(21, isHereDocType('taml'));

utest.falsy(22, isHereDocType('two line'));

// ---------------------------------------------------------------------------
utest.equal(26, lineToParts('this is not a heredoc'), ['this is not a heredoc']);

utest.equal(30, lineToParts('this <<< is <<< heredoc'), ['this ', '<<<', ' is ', '<<<', ' heredoc']);

utest.equal(38, lineToParts('<<< is <<< heredoc'), ['', '<<<', ' is ', '<<<', ' heredoc']);

utest.equal(46, lineToParts('this <<< is <<<'), ['this ', '<<<', ' is ', '<<<', '']);

utest.equal(54, lineToParts('<<< is <<<'), ['', '<<<', ' is ', '<<<', '']);

utest.equal(62, lineToParts('<<<'), ['', '<<<', '']);

utest.equal(68, lineToParts('<<<<<<'), ['', '<<<', '', '<<<', '']);

// ---------------------------------------------------------------------------
utest.equal(78, mapHereDoc(`abc
def`), '"abc\\ndef"');

// ---------------------------------------------------------------------------
utest.equal(86, mapHereDoc(`===
abc
def`), '"abc\\ndef"');

// ---------------------------------------------------------------------------
utest.equal(95, mapHereDoc(`...
abc
def`), '"abc def"');

// ---------------------------------------------------------------------------
utest.equal(104, mapHereDoc(`() -> count += 1`), '() -> count += 1');

// ---------------------------------------------------------------------------
utest.equal(111, mapHereDoc(`---
a: 1
b: 2`), '{"a":1,"b":2}');

// ---------------------------------------------------------------------------
utest.equal(120, mapHereDoc(`---
- a
- b`), '["a","b"]');

// ---------------------------------------------------------------------------
HereDocTester = class HereDocTester extends UnitTester {
  transformValue(block) {
    return mapHereDoc(block);
  }

};

docTester = new HereDocTester();

// ------------------------------------------------------------------------
// Default heredoc type is a block
docTester.equal(140, `this is a
block of text`, '"this is a\\nblock of text"');

// ------------------------------------------------------------------------
// Make explicit that the heredoc type is a block
docTester.equal(149, `===
this is a
block of text`, '"this is a\\nblock of text"');

// ------------------------------------------------------------------------
// One Line block
docTester.equal(159, `...this is a
line of text`, '"this is a line of text"');

// ------------------------------------------------------------------------
// One Line block
docTester.equal(168, `...
this is a
line of text`, '"this is a line of text"');

// ---------------------------------------------------------------------------
// Test creating new heredoc types
MatrixHereDoc = class MatrixHereDoc extends BaseHereDoc {
  map(block) {
    var i, lArray, len, line, ref, result;
    // --- if block starts with a digit
    dbgEnter("MatrixHereDoc.map", block);
    if (notdefined(block.match(/^\s*\d/s))) {
      dbgReturn("MatrixHereDoc.map", undef);
      return undef;
    }
    lArray = [];
    ref = blockToArray(block);
    for (i = 0, len = ref.length; i < len; i++) {
      line = ref[i];
      lArray.push(extractMatches(line, /\d+/g, parseInt));
    }
    result = JSON.stringify(lArray);
    dbgReturn("MatrixHereDoc.map", result);
    return result;
  }

};

addHereDocType('matrix', MatrixHereDoc);

docTester.equal(195, `1 2 3
2 4 6`, '[[1,2,3],[2,4,6]]');

// ------------------------------------------------------------------------
// Test creating a new heredoc type by overriding mapToString
UCHereDoc = class UCHereDoc extends BaseHereDoc {
  map(block) {
    var result;
    dbgEnter("UCHereDoc.map", block);
    if (block.indexOf('^^^') !== 0) {
      dbgReturn("UCHereDoc.map", undef);
      return undef;
    }
    block = block.substring(4).toUpperCase();
    dbg('block', block);
    result = JSON.stringify(block);
    dbgReturn("UCHereDoc.map", result);
    return result;
  }

};

addHereDocType('upper case', UCHereDoc);

docTester.equal(221, `^^^
This is a
block of text`, '"THIS IS A\\nBLOCK OF TEXT"');

// ---------------------------------------------------------------------------
// --- test creating a custom HEREDOC section

//     e.g. with header line ***,
//     we'll create an upper-cased single line string
UCHereDoc2 = class UCHereDoc2 extends BaseHereDoc {
  map(block) {
    var result;
    dbgEnter("UCHereDoc2.map", block);
    if (firstLine(block) !== '***') {
      dbgReturn("UCHereDoc.map", undef);
      return undef;
    }
    block = CWS(remainingLines(block).toUpperCase());
    dbg('block', block);
    result = JSON.stringify(block);
    dbgReturn("UCHereDoc2.map", result);
    return result;
  }

};

addHereDocType('upper case 2', UCHereDoc2);

// ---------------------------------------------------------------------------
docTester.equal(253, `***
select ID,Name
from Users`, '"SELECT ID,NAME FROM USERS"');

// ===========================================================================
HereDocTester = class HereDocTester extends UnitTester {
  transformValue(block) {
    return mapHereDoc(block);
  }

};

docTester = new HereDocTester();

// ---------------------------------------------------------------------------
// TAML block
docTester.equal(272, `---
- abc
- def`, '["abc","def"]');

// ---------------------------------------------------------------------------
// TAML-like block, but actually a block
docTester.equal(282, `===
---
- abc
- def`, '"---\\n- abc\\n- def"');

// ---------------------------------------------------------------------------
// TAML block 2
docTester.equal(293, `---
-
	label: Help
	url: /help
-
	label: Books
	url: /books`, '[{"label":"Help","url":"/help"},{"label":"Books","url":"/books"}]');

// ---------------------------------------------------------------------------
HereDocReplacer = class HereDocReplacer extends UnitTester {
  transformValue(block) {
    var lNewParts, part, result;
    lNewParts = (function() {
      var i, len, ref, results;
      ref = lineToParts(firstLine(block));
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        part = ref[i];
        if (part === '<<<') {
          results.push(mapHereDoc(undented(remainingLines(block))));
        } else {
          results.push(part); // keep as is
        }
      }
      return results;
    })();
    result = lNewParts.join('');
    return result;
  }

};

replacer = new HereDocReplacer();

// ---------------------------------------------------------------------------
replacer.equal(322, `TopMenu lItems={<<<}
	---
	-
		label: Help
		url: /help
	-
		label: Books
		url: /books`, `TopMenu lItems={[{"label":"Help","url":"/help"},{"label":"Books","url":"/books"}]}`);

// ---------------------------------------------------------------------------
replacer.equal(337, `<TopMenu lItems={<<<}>
	---
	-
		label: Help
		url: /help
	-
		label: Books
		url: /books`, `<TopMenu lItems={[{"label":"Help","url":"/help"},{"label":"Books","url":"/books"}]}>`);

// ---------------------------------------------------------------------------
(function() {
  var HereDocMapper;
  HereDocMapper = class HereDocMapper extends UnitTester {
    transformValue(block) {
      return mapHereDoc(block);
    }

  };
  docTester = new HereDocMapper();
  // ------------------------------------------------------------------------
  docTester.equal(363, `(evt) ->
	LOG 'click'`, `(evt) ->
	LOG 'click'`);
  // ------------------------------------------------------------------------
  // Function block, with no name or parameters
  docTester.equal(375, `() ->
	return true`, `() ->
	return true`);
  // ------------------------------------------------------------------------
  // Function block, with no name but one parameter
  docTester.equal(386, `(evt) ->
	console.log 'click'`, `(evt) ->
	console.log 'click'`);
  // ------------------------------------------------------------------------
  // Function block, with no name but one parameter
  return docTester.equal(397, `(  evt  )     ->
	LOG 'click'`, `(  evt  )     ->
	LOG 'click'`);
})();
